1.A有了构造函数后编译器就不会自动生成默认构造函数  B与默认构造函数冲突  D析构函数不能有参数
2.洋葱式结构，先创建成员变量然后创建对象将其包起来，析构时也是先析构对象再析构成员变量
4.&为左值引用，&&为右值引用，无论是左值引用还是右值引用，引用都是左值
5.A将School写作Student里的friend，指的是School可以调用Student内的private而Student不能调用School的private
BC析构函数被置为私有域时，只有在自己的或友元的成员函数内才能被析构，否则会报错
6.A bob为const对象，初始化只有两种方式，就地初始化或初始化列表初始化，其中gpa为就地初始化而id不是
B const对象只能调用const函数 D const函数只是不能修改任何参数的值，但是可以访问
7.A static数据成员只能在类中声名，实现文件中定义，且需要在main函数之前完成初始化。
	若static在类中就地初始化，则多次包含类所在文件的时候可能引起重定义
	B对于static成员变量a，有两种访问方式，类名::a或者对象名.a
	C 
8.A new[]必须用delete[]，否则会导致内存泄漏
C常量静态变量需要在类外初始化，int和enum除外
D指针被delete后还是指向原来的堆空间，并非nullptr